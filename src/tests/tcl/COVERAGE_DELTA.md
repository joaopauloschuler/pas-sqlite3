# COVERAGE_DELTA.md

Generated by `bin/TclTestDriver --coverage` (Phase 9.4.8.d).

Opcodes hit by the tcl-feature corpus that are catalogued cold
in `src/tests/corpus/COVERAGE_GAPS.md` (i.e. unreached by the
9.1 / 9.2 .pas spine corpus).  These are the opcodes only the
Tcl driver exercises today.  When the 9.1 corpus closes one of
these gaps it should be dropped from `IsCoverageGap` upstream.

| opcode | name | tcl-hits |
|-------:|------|---------:|
| 24 | SeekGT | 5 |

_Summary: 94 opcodes hit by the tcl corpus; 1 of them are catalogued cold in the .pas corpus._
